'use client';

import { useEffect, useMemo, useState } from 'react';
import { createClient, type Session } from '@supabase/supabase-js';

const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL || 'https://lzuyaaxmjnklhjutxbio.supabase.co', process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY || 'sb_publishable__WZhhkjiKnCqmqspjL5tbQ_9o1fHoC8', { auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true} });
type Talent={id:string;talent_id:string;nom:string;univers:string|null;image:string|null;pays:string|null;age:number|null;audience:Record<string,unknown>|null;reseaux:Record<string,unknown>|null;manager:string|null;contact:string|null;collaborations:unknown;adn:Record<string,unknown>|null};
type Maison={id:string;maison_id:string;nom:string;univers:string|null;positionnement:string|null;gamme_prix:string|null;pays:string|null;site_officiel:string|null};
type Match={talent_id:string;maison_id:string;score:number;recommandation:string|null;raisons:unknown};
type Tab='Dashboard'|'Talents'|'Maisons'|'Matching'|'Briefs';
const norm=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const logoSlugs:Record<string,string>={Rolex:'rolex',Cartier:'cartier','Audemars Piguet':'audemarspiguet','Patek Philippe':'patekphilippe','Richard Mille':'richardmille',Hublot:'hublot',Omega:'omega','TAG Heuer':'tagheuer',Breitling:'breitling',Bulgari:'bulgari','IWC Schaffhausen':'iwc','Jaeger-LeCoultre':'jaegerlecoultre',Longines:'longines',Panerai:'panerai',Seiko:'seiko',Citizen:'citizen','G-Shock':'gshock'};
function TalentImage({name,src}:{name:string;src?:string|null}){const[img,setImg]=useState(src||null);useEffect(()=>{if(src)return;let dead=false;fetch(`https://fr.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(name)}`).then(r=>r.ok?r.json():null).then(x=>{if(!dead)setImg(x?.thumbnail?.source||null)}).catch(()=>{});return()=>{dead=true}},[name,src]);return img?<img className="talent-photo" src={img} alt={name}/>:<div className="avatar">{name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div>}
function HouseLogo({name}:{name:string}){const[broken,setBroken]=useState(false);const slug=logoSlugs[name];const url=slug?`https://cdn.simpleicons.org/${slug}`:null;return url&&!broken?<img className="house-logo" src={url} alt={`${name} logo`} onError={()=>setBroken(true)}/>:<div className="watch">{name.split(/\s+/).map(x=>x[0]).slice(0,3).join('').toUpperCase()}</div>}
function jsonText(v:unknown){if(!v)return '';if(typeof v==='string')return v;try{return JSON.stringify(v).replace(/[{}\[\]"]/g,'').replace(/[:,]/g,' · ')}catch{return ''}}
function Login({onLogged}:{onLogged:(s:Session)=>void}){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');const r=await supabase.auth.signInWithPassword({email,password});if(r.error)setError('Email ou mot de passe incorrect.');else if(r.data.session)onLogged(r.data.session);setBusy(false)};return <main className="auth-shell"><div className="auth-glow"/><section className="auth-card"><div className="gz-mark">GZ</div><div className="logo auth-logo">GZ<span>INTELLIGENCE</span></div><p className="eyebrow">PRIVATE WORKSPACE · GZ AGENCY</p><h1>Bienvenue.</h1><p className="sub">Connecte-toi pour accéder à la base talents, aux maisons horlogères et au matching GZ.</p><form onSubmit={submit} className="auth-form"><label>Email professionnel<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="nom@agence.com"/></label><label>Mot de passe<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button className="primary auth-submit" disabled={busy}>{busy?'Connexion…':'Se connecter'}</button></form>{error&&<p className="error auth-error">{error}</p>}<div className="divider"><span>ACCÈS PRIVÉ</span></div><p className="access-note">Les comptes sont créés par GZ. Il n’y a pas d’inscription publique.</p></section></main>}
export default function Home(){const[session,setSession]=useState<Session|null>(null),[ready,setReady]=useState(false),[talents,setTalents]=useState<Talent[]>([]),[maisons,setMaisons]=useState<Maison[]>([]),[matches,setMatches]=useState<Match[]>([]),[query,setQuery]=useState(''),[tab,setTab]=useState<Tab>('Dashboard'),[selected,setSelected]=useState<Talent|null>(null),[loading,setLoading]=useState(false),[error,setError]=useState('');
useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setReady(true)});const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>subscription.unsubscribe()},[]);
useEffect(()=>{if(!session)return;setLoading(true);Promise.all([supabase.from('talents').select('id,talent_id,nom,univers,image,pays,age,audience,reseaux,manager,contact,collaborations,adn').order('nom'),supabase.from('maisons').select('id,maison_id,nom,univers,positionnement,gamme_prix,pays,site_officiel').order('nom'),supabase.from('matching_gz').select('talent_id,maison_id,score,recommandation,raisons').order('score',{ascending:false})]).then(([a,b,c])=>{const first=a.error||b.error||c.error;if(first)setError(first.message);setTalents(a.data||[]);setMaisons(b.data||[]);setMatches((c.data||[]).map(x=>({...x,score:Number(x.score)})))}).finally(()=>setLoading(false))},[session]);
const q=norm(query);const talentResults=useMemo(()=>{if(!q)return[];const seen=new Set<string>();return talents.filter(t=>norm(t.nom).includes(q)).filter(t=>{const k=norm(t.nom);if(seen.has(k))return false;seen.add(k);return true})},[talents,q]);const houseResults=useMemo(()=>q?maisons.filter(m=>norm(m.nom).includes(q)):[],[maisons,q]);
if(!ready)return <div className="loading">Initialisation sécurisée…</div>;if(!session)return <Login onLogged={setSession}/>;const signOut=()=>supabase.auth.signOut();const maison=(id:string)=>maisons.find(m=>m.id===id||m.maison_id===id);const selectedMatches=selected?matches.filter(m=>m.talent_id===selected.id||m.talent_id===selected.talent_id).slice(0,10):[];
return <main className="shell"><aside className="sidebar"><div className="logo">GZ<span>INTELLIGENCE</span></div><nav>{['Dashboard','Talents','Maisons','Matching','Briefs'].map(x=><button key={x} className={tab===x?'active':''} onClick={()=>setTab(x as Tab)}>{x}</button>)}</nav><div className="status"><i/> Accès authentifié<br/><span>{session.user.email}</span></div><button className="mini" onClick={signOut}>Se déconnecter</button></aside><section className="content"><header><div><p className="eyebrow">GZ AGENCY · PRIVATE INTELLIGENCE</p><h1>Le cerveau du deal.</h1><p className="sub">Recherche à la demande. Aucun talent n’est affiché tant que tu ne le recherches pas.</p></div></header><div className="search"><span>⌕</span><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Rechercher un artiste ou une maison…"/></div>
{query&&<section className="panel search-results"><div className="search-title"><span>RECHERCHE GZ</span><span>{talentResults.length+houseResults.length} résultat(s)</span></div>{talentResults.length===0&&houseResults.length===0?<p className="sub">Aucun résultat. Essaie le nom exact.</p>:<div className="talent-list">{talentResults.map(t=><button className="talent-card" key={t.id} onClick={()=>{setSelected(t);setTab('Matching')}}><TalentImage name={t.nom} src={t.image}/><span><b>{t.nom}</b><small>{t.univers||'Talent'}</small></span><em>Ouvrir →</em></button>)}{houseResults.map(m=><button className="talent-card" key={m.id} onClick={()=>setTab('Maisons')}><HouseLogo name={m.nom}/><span><b>{m.nom}</b><small>{m.positionnement||m.univers||'Maison horlogère'}</small></span><em>Maison →</em></button>)}</div>}</section>}
{error&&<div className="panel error-panel">{error}</div>}{loading?<div className="loading">Connexion à la base GZ…</div>:!query&&tab==='Dashboard'?<section className="grid stats" style={{marginTop:28}}><article><small>TALENTS</small><strong>{talents.length}</strong><p>Disponibles par recherche</p></article><article><small>MAISONS</small><strong>{maisons.length}</strong><p>Disponibles par recherche</p></article><article><small>MATCHINGS</small><strong>{matches.length}</strong><p>Calculés dans GZ</p></article><article><small>ACCÈS</small><strong>PRIVÉ</strong><p>Associés GZ</p></article></section>:null}
{tab==='Talents'&&!query&&<section className="panel full"><div className="panel-head"><div><small>GZ TALENT DATABASE</small><h2>Recherche requise</h2></div></div><p className="sub">Tape le nom d’un talent dans la barre de recherche.</p></section>}
{tab==='Maisons'&&!query&&<section className="panel full"><div className="panel-head"><div><small>WATCH HOUSE DATABASE</small><h2>Recherche requise</h2></div></div><p className="sub">Tape le nom d’une maison pour afficher sa fiche et son identité visuelle.</p></section>}
{tab==='Matching'&&<section className="panel full"><div className="panel-head"><div><small>GZ MATCHING ENGINE</small><h2>{selected?`Fiche & matching · ${selected.nom}`:'Recherche d’abord un talent'}</h2></div></div>{selected?<><div className="profile" style={{marginTop:18}}><TalentImage name={selected.nom} src={selected.image}/><b>{selected.nom}</b><p>{selected.univers||'Talent'} · {selected.pays||'France'}{selected.age?` · ${selected.age} ans`:''}</p>{selected.manager&&<p>Manager : {selected.manager}</p>}{selected.contact&&<p>Contact : {selected.contact}</p>}</div><div className="match-list">{selectedMatches.map(m=>{const h=maison(m.maison_id);return h?<div className="match" key={`${m.talent_id}-${m.maison_id}`}><HouseLogo name={h.nom}/><div><b>{h.nom}</b><p>{m.recommandation||jsonText(m.raisons)||'Compatibilité stratégique GZ'}</p></div><strong>{Math.round(m.score)}%</strong></div>:null})}{selectedMatches.length===0&&<p className="sub">Aucun matching calculé pour ce profil.</p>}</div></>:<p className="sub">Recherche un talent, puis ouvre sa fiche pour voir les maisons compatibles.</p>}</section>}
{tab==='Briefs'&&<section className="panel full"><div className="panel-head"><div><small>DEAL PIPELINE</small><h2>Briefs GZ</h2></div></div><p className="sub">Les briefs de tes associés apparaîtront ici.</p></section>}
</section></main>}
