'use client';
import { useEffect, useMemo, useState } from 'react';
const SUPABASE_URL='https://lzuyaaxmjnklhjutxbio.supabase.co'; const SUPABASE_KEY='sb_publishable__WZhhkjiKnCqmqspjL5tbQ_9o1fHoC8';
type Talent={id:string;talent_id:string;nom:string;univers:string|null;image:string|null;pays:string|null;adn:Record<string,unknown>|null;marques_compatibles:string[]|null};
type Maison={id:string;maison_id:string;nom:string;univers:string|null;positionnement:string|null;gamme_prix:string|null};
type Match={talent_id:string;maison_id:string;score:number;recommandation:string|null};
async function api<T>(table:string,params=''):Promise<T>{const r=await fetch(`${SUPABASE_URL}/rest/v1/${table}?${params}`,{headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`},cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()}
function WikiImage({name}:{name:string}){const[src,setSrc]=useState<string|null>(null);useEffect(()=>{fetch(`https://fr.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(name)}`).then(r=>r.ok?r.json():null).then(x=>setSrc(x?.thumbnail?.source??null)).catch(()=>{})},[name]);return src?<img className="talent-photo" src={src} alt={name}/>:<div className="avatar">{name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div>}
export default function Home(){const[talents,setTalents]=useState<Talent[]>([]),[maisons,setMaisons]=useState<Maison[]>([]),[matches,setMatches]=useState<Match[]>([]),[query,setQuery]=useState(''),[tab,setTab]=useState('Dashboard'),[selectedTalent,setSelectedTalent]=useState<Talent|null>(null),[loading,setLoading]=useState(true);
useEffect(()=>{Promise.all([api<Talent[]>('talents','select=id,talent_id,nom,univers,image,pays,adn,marques_compatibles&order=nom'),api<Maison[]>('maisons','select=id,maison_id,nom,univers,positionnement,gamme_prix&order=nom'),api<Match[]>('matching_gz','select=talent_id,maison_id,score,recommandation&order=score.desc')]).then(([a,b,c])=>{setTalents(a);setMaisons(b);setMatches(c)}).finally(()=>setLoading(false))},[]);
const filteredTalents=useMemo(()=>talents.filter(t=>t.nom.toLowerCase().includes(query.toLowerCase())||t.univers?.toLowerCase().includes(query.toLowerCase())),[talents,query]);const filteredMaisons=useMemo(()=>maisons.filter(m=>m.nom.toLowerCase().includes(query.toLowerCase())||m.univers?.toLowerCase().includes(query.toLowerCase())),[maisons,query]);const getMaison=(id:string)=>maisons.find(m=>m.id===id);const selectedMatches=selectedTalent?matches.filter(m=>m.talent_id===selectedTalent.id).sort((a,b)=>b.score-a.score).slice(0,8):[];
return <main className="shell"><aside className="sidebar"><div className="logo">GZ<span>INTELLIGENCE</span></div><nav>{['Dashboard','Talents','Maisons','Matching','Briefs','Base GZ'].map(x=><a key={x} className={tab===x?'active':''} onClick={()=>setTab(x)}>{x}</a>)}</nav><div className="status"><i/> Système opérationnel</div></aside><section className="content"><header><div><p className="eyebrow">GZ AGENCY · PRIVATE INTELLIGENCE</p><h1>Le cerveau du deal.</h1><p className="sub">Intelligence horlogère, culture urbaine et matching stratégique.</p></div><button onClick={()=>setTab('Briefs')}>+ Nouveau brief</button></header><div className="search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Rechercher un artiste, une maison ou un univers…"/></div>{loading?<div className="loading">Connexion à la base GZ…</div>:<>{tab==='Dashboard'&&<><section className="grid stats"><article><small>TALENTS</small><strong>{talents.length}</strong><p>Profils GZ indexés</p></article><article><small>MAISONS</small><strong>{maisons.length}</strong><p>Maisons documentées</p></article><article><small>MATCHINGS</small><strong>{matches.length}</strong><p>Connexions calculées</p></article><article><small>BRIEFS</small><strong>18</strong><p>Pipeline GZ</p></article></section><section className="grid main-grid"><article className="panel"><div className="panel-head"><div><small>INTELLIGENCE</small><h2>Matching express</h2></div><span>GZ AI</span></div><p className="question">Sélectionne un talent pour voir ses maisons prioritaires.</p><div className="talent-list">{talents.map(t=><button className="talent-card" key={t.id} onClick={()=>{setSelectedTalent(t);setTab('Matching')}}><WikiImage name={t.nom}/><span><b>{t.nom}</b><small>{t.univers}</small></span><em>→</em></button>)}</div></article><article className="panel"><div className="panel-head"><div><small>MAISONS</small><h2>Maisons suivies</h2></div><span>{maisons.length} actives</span></div>{maisons.slice(0,8).map(m=><div className="row" key={m.id}><div className="watch">{m.nom.slice(0,2).toUpperCase()}</div><div><b>{m.nom}</b><p>{m.positionnement||m.univers||'Profil stratégique GZ'}</p></div><strong>GZ</strong></div>)}</article></section></>}{tab==='Talents'&&<section className="panel full"><div className="panel-head"><div><small>GZ TALENT DATABASE</small><h2>Talents</h2></div><span>{filteredTalents.length} résultats</span></div><div className="cards">{filteredTalents.map(t=><button className="profile" key={t.id} onClick={()=>{setSelectedTalent(t);setTab('Matching')}}><WikiImage name={t.nom}/><b>{t.nom}</b><p>{t.univers||'—'}</p><small>{t.pays||'France'}</small></button>)}</div></section>}{tab==='Maisons'&&<section className="panel full"><div className="panel-head"><div><small>WATCH HOUSE DATABASE</small><h2>Maisons horlogères</h2></div><span>{filteredMaisons.length} résultats</span></div><div className="cards">{filteredMaisons.map(m=><div className="profile" key={m.id}><div className="watch big">{m.nom.slice(0,2).toUpperCase()}</div><b>{m.nom}</b><p>{m.positionnement||m.univers||'—'}</p><small>{m.gamme_prix||'—'}</small></div>)}</div></section>}{tab==='Matching'&&<section className="panel full"><div className="panel-head"><div><small>MATCHING ENGINE</small><h2>{selectedTalent?`Maisons compatibles · ${selectedTalent.nom}`:'Sélectionne un talent'}</h2></div></div>{selectedTalent?<div className="match-list">{selectedMatches.map(m=>{const h=getMaison(m.maison_id);return h&&<div className="match" key={m.maison_id}><div className="watch">{h.nom.slice(0,2).toUpperCase()}</div><div><b>{h.nom}</b><p>{m.recommandation}</p></div><strong>{Math.round(m.score)}%</strong></div>})}</div>:<div className="cards">{talents.map(t=><button className="profile" key={t.id} onClick={()=>setSelectedTalent(t)}><WikiImage name={t.nom}/><b>{t.nom}</b><p>Voir les matchings →</p></button>)}</div>}</section>}{tab==='Briefs'&&<section className="panel full"><div className="panel-head"><div><small>DEAL PIPELINE</small><h2>Briefs</h2></div><button className="mini">+ Créer</button></div><div className="brief"><b>Campagne horlogère · Culture urbaine</b><p>Identifier 5 profils pertinents selon maison, image, audience et univers.</p><span>ACTIF · Recherche GZ</span></div><div className="brief"><b>Matching maison ↔ talent</b><p>Analyse stratégique et shortlist pour négociation.</p><span>ACTIF · GZ Intelligence</span></div></section>}{tab==='Base GZ'&&<section className="panel full"><div className="panel-head"><div><small>MASTER DATABASE</small><h2>Base GZ</h2></div></div><p className="sub">Talents, maisons, matchings et briefs sont reliés à la base Supabase GZ Intelligence.</p><div className="data-summary"><b>{talents.length}</b><span>Talents</span><b>{maisons.length}</b><span>Maisons</span><b>{matches.length}</b><span>Matchings</span></div></section>}</>}</section></main>}
